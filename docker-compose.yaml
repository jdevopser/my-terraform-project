version: '3.7'
services:
  jenkins:
    image: jenkins/jenkins:lts
    privileged: true
    user: root
    ports:
      - 8080:8080
      - 50000:50000
    container_name: my-jenkins
    volumes:
      - /mnt/c/Users/Dell/Desktop/my-terraform-project/jenkins-data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    
    

 
    
   
  
